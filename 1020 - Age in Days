#include <stdio.h>
 
int main() {
 
    int y, m, d, r_y;
    scanf("%d", &d);
    
    y = d/365;
    r_y = d%365;
    m = r_y/30;
    d = r_y%30;
    
    printf("%d ano(s)\n", y);
    printf("%d mes(es)\n", m);
    printf("%d dia(s)\n", d);

    return 0;
}
